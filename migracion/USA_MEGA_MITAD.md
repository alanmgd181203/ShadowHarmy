# USA — mega bóveda mitad propia (AAL→BITO)

Tras el split con Jess, **esta lap** solo remata:

`data/coliseo/mega_boveda/lista_usa_resto.txt` (183 bases · AAL→BITO)

```powershell
# Parar el mega viejo (catálogo completo) si sigue vivo, luego:
python -u scripts/coliseo_mega_boveda.py --dias 365 --workers 2 --sleep 0.12 --watchdog --only-file data/coliseo/mega_boveda/lista_usa_resto.txt
```

Jess: ver `migracion/ORDEN_ACTIVA_JESS.md`.  
Merge: `data/coliseo/INSTRUCCIONES_MERGE_MEGA.md`.
