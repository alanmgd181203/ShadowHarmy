# USA — mega cola SUSHI→VET (38)

Jess casi terminó AAL→BITO; se partió lo que faltaba de BLEND→ZRX:

- **USA:** `lista_usa_blend_cola.txt` · SUSHI → VET (38)  
- **Jess:** `lista_jess_extra_blend.txt` · W → ZRX (38)  
- Sello: `split_blend_cola_usa_jess.json`

```powershell
python -u scripts/coliseo_mega_boveda.py --dias 365 --workers 2 --sleep 0.12 --watchdog --only-file data/coliseo/mega_boveda/lista_usa_blend_cola.txt
```

Jess: `migracion/ORDEN_ACTIVA_JESS.md`.  
Merge: `data/coliseo/INSTRUCCIONES_MERGE_MEGA.md`.
