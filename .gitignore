# Secretos — .env testnet incluido en repo para despliegue; no subir .env.local
.env.local
.env.*
!.env

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# Estado temporal
*.tmp
data/*.tmp

# Coliseo — bóveda pesada (Drive, no git)
data/coliseo/*.sqlite
data/coliseo/*.sqlite-*
data/coliseo/*.zip
data/coliseo/heartbeat.json
data/coliseo/checkpoint.json

# IDE (opcional local)
.vscode/
